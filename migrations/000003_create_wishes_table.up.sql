CREATE TABLE IF NOT EXISTS wish (
                                      id bigserial PRIMARY KEY,
                                      title text NOT NULL,
                                    price integer NOT NULL
);